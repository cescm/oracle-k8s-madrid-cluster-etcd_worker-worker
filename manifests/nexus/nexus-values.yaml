replicaCount: 1
persistence:
  enabled: true
  size: 50Gi
  storageClass: local-path
service:
  type: ClusterIP
ingress:
  enabled: false
resources:
  requests:
    cpu: 500m
    memory: 1Gi
  limits:
    cpu: "1"
    memory: 2Gi
nodeSelector:
  kubernetes.io/hostname: madrid-wk-01
